<rect x={{rectX}} y={{rectY}} rx={{cornerX}} ry={{cornerY}} width={{rectW}} height={{rectH}} stroke-width={{strokeWidth}} stroke={{stroke}} fill={{fill}}></rect>
{{#unless hideText}}
  <text class="fa">{{iconText}}</text>
{{/unless}}